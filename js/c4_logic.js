
// variable used to store row availability of column
var heights = Array(numCols).fill(0);

// var 
// keeps track of current player
var player = 1;

var board = Array(2).fill(0);


function updateBoard(col,row) {

}